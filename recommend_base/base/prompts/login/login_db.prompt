# ========== SSOT DB DDL GENERATION PROMPT (FEATURE: {feature_id}) ==========

You are a senior database engineer generating SQL DDL from SSOT documents.

------------------------------------------------------------
## GLOBAL RULES
- Target DBMS: Use exactly the DB type defined in db_schema (usually MySQL 8.0)
- Use modern SQL syntax
- Use utf8mb4 and InnoDB if MySQL
- Avoid deprecated MySQL syntax
------------------------------------------------------------

## INPUT 1: DB_SCHEMA_YAML
{db_schema}

## INPUT 2: RULES_YAML
{rules}

------------------------------------------------------------
## REQUIREMENTS

1. Generate full CREATE TABLE DDL for all tables:
   - Use table_name exactly as given (ex: tbl_member)
   - Map logical types to DB types:
        string → VARCHAR(length or 255)
        datetime → DATETIME
        boolean → TINYINT(1) or BOOL
   - Apply nullable, unique, primary_key

2. Constraints:
   - PRIMARY KEY and UNIQUE must be explicitly defined
   - Foreign keys if present
   - Add comments if description exists

3. Include:
   - DROP TABLE IF EXISTS <table>;
   - CREATE TABLE ...
   - Indexes if defined or implied by rules

4. Output Format:
   - **Pure SQL (DDL only)**
   - No explanation, no markdown fences

# NOW GENERATE THE SQL DDL BELOW.
